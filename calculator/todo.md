# Plan for adding Exponentiation to Calculator

- [x] Update `pkg/calculator.py`
    - [x] Add `^` (exponent) operator to `self.operators` dictionary.
    - [x] Add `^` (exponent) operator to `self.precedence` dictionary (set precedence higher than `*` and `/`, typically 3).
- [x] Add unit tests for exponentiation to `tests.py`.
- [x] Verify functionality.
